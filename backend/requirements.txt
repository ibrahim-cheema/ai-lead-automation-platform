# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn==0.30.1
python-multipart==0.0.9

# ── Pydantic & validation ─────────────────────────────────────────────────────
pydantic>=2.7.4
pydantic-settings>=2.3.3
email-validator>=2.1.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# ── AI providers ──────────────────────────────────────────────────────────────
openai>=1.34.0                # OpenAI, Groq, xAI, HF (OpenAI-compatible endpoints)
anthropic>=0.50.0             # AI Agent orchestration (Claude Haiku tool-use)
huggingface-hub>=0.24.0       # GPT-OSS-120B and other HF models

# ── MCP (Model Context Protocol) ──────────────────────────────────────────────
mcp[cli]>=1.0.0               # FastMCP server + MCP client
